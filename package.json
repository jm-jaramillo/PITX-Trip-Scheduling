{
  "name": "pitx-bay-booking",
  "version": "1.0.0",
  "private": true,
  "description": "PITX bus bay hourly booking - static site in docs/ backed by Supabase. This package.json exists only for the setup/helper scripts; the site itself has no build step.",
  "scripts": {
    "serve": "node scripts/serve-docs.mjs 3100",
    "create-staff": "node scripts/create-staff.mjs",
    "create-operator": "node scripts/create-operator.mjs",
    "delete-account": "node scripts/delete-account.mjs",
    "migrate": "node scripts/run-migration.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.23.0"
  }
}
